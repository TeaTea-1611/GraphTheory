import { Graph } from "./TypeGraph";

const dijkstraAlgorithm = (graph: Graph, start: string) => {
  // const d = Object.keys(graph).fill(Number.MAX_SAFE_INTEGER)
};

export default dijkstraAlgorithm;
